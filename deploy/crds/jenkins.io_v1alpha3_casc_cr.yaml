apiVersion: jenkins.io/v1alpha3
kind: Casc
metadata:
  annotations:
    jenkins.io/jenkins-reference: 'example'
  name: example-casc
spec:
  configurationAsCode:
    configurations: 
    - name: jenkins-operator-user-configuration
  groovyScripts:
    configurations:
    - name: jenkins-operator-user-configuration
